using UnityEngine;
using UnityEngine.SceneManagement;

public class LevelReloadManager : MonoBehaviour
{
    //[SerializeField] private PlayerMovement playerMovement;
    //[SerializeField] private CloneMovement cloneMovement;
    //[SerializeField] private GameObject clone;

    //private void Awake()
    //{
    //    PlayerMoves previousMoves = SavingManager.Load();
    //    if (previousMoves != null)
    //    {
    //        cloneMovement.olderMoves = previousMoves;
    //        clone.SetActive(true);
    //    }
    //}
    //public void ReloadLevel()
    //{
    //    SavingManager.Save(new PlayerMoves(playerMovement.movementsQueue));
    //    SceneManager.LoadScene(SceneManager.GetActiveScene().buildIndex);
    //}
}
